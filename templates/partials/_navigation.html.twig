<div x-data="{ isOpen: true }" class="relative z-50">
    <!-- Mobile menu button -->
    <button @click="isOpen = !isOpen"
            class="p-2 text-gray-300 hover:text-white xxl:hidden"
            :aria-expanded="isOpen">
        <span x-show="!isOpen">☰ Menu</span>
        <span x-show="isOpen" x-cloak>✕ Close</span>
    </button>
    <!-- Desktop menu left
    <div class="xxl:flex items-center space-x-8 ml-10">
        <div class="flex items-center space-x-4">
            <a href="{{ path('home') }}"
               class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:bg-slate-800/50 hover:scale-105"
               :class="{ 'text-white bg-slate-800/50': app.request.attributes.get('_route') == 'home' }">
                Home
            </a>
            <a href="{{ path('about') }}"
               class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:bg-slate-800/50 hover:scale-105"
               :class="{ 'text-white bg-slate-800/50': app.request.attributes.get('_route') == 'about' }">
                About
            </a>
            <div x-data="{ servicesOpen: false }" class="relative">
                <button @click="servicesOpen = !servicesOpen"
                        class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:bg-slate-800/50 hover:scale-105 flex items-center"
                        :class="{ 'text-white bg-slate-800/50': ['ai_optimization', 'ai_automation', 'ai_marketing', 'ai_personnel'].includes(app.request.attributes.get('_route')) }">
                    Services
                    <svg class="ml-1 h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                </button>

                <div x-show="servicesOpen"
                     @click.away="servicesOpen = false"
                     x-transition:enter="transition ease-out duration-200"
                     x-transition:enter-start="opacity-0 scale-95"
                     x-transition:enter-end="opacity-100 scale-100"
                     x-transition:leave="transition ease-in duration-150"
                     x-transition:leave-start="opacity-100 scale-100"
                     x-transition:leave-end="opacity-0 scale-95"
                     class="origin-top-left absolute left-0 mt-2 w-48 rounded-md shadow-lg bg-slate-800/95 backdrop-blur-md ring-1 ring-slate-700/50 z-50">
                    <div class="py-1">
                        <a href="{{ path('ai_optimization') }}"
                           class="block px-4 py-2 text-sm text-slate-300 hover:text-white hover:bg-slate-700/50 transition-colors duration-300"
                           :class="{ 'text-white bg-slate-700/50': app.request.attributes.get('_route') == 'ai_optimization' }">
                            AI Optimization
                        </a>
                        <a href="{{ path('ai_automation') }}"
                           class="block px-4 py-2 text-sm text-slate-300 hover:text-white hover:bg-slate-700/50 transition-colors duration-300"
                           :class="{ 'text-white bg-slate-700/50': app.request.attributes.get('_route') == 'ai_automation' }">
                            AI Automation
                        </a>
                        <a href="{{ path('ai_marketing') }}"
                           class="block px-4 py-2 text-sm text-slate-300 hover:text-white hover:bg-slate-700/50 transition-colors duration-300"
                           :class="{ 'text-white bg-slate-700/50': app.request.attributes.get('_route') == 'ai_marketing' }">
                            AI Marketing
                        </a>
                        <a href="{{ path('ai_personnel') }}"
                           class="block px-4 py-2 text-sm text-slate-300 hover:text-white hover:bg-slate-700/50 transition-colors duration-300"
                           :class="{ 'text-white bg-slate-700/50': app.request.attributes.get('_route') == 'ai_personnel' }">
                            AI Personnel
                        </a>
                    </div>
                </div>
            </div>
            <a href="{{ path('contact') }}"
               class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:bg-slate-800/50 hover:scale-105"
               :class="{ 'text-white bg-slate-800/50': app.request.attributes.get('_route') == 'contact' }">
                Contact
            </a>
        </div>
    </div>

    <!-- Logo / Brand
    <div class="flex-shrink-0 flex items-center">
        <a href="{{ path('home') }}" class="text-xl font-bold metallic-text hover:scale-105 transition-transform duration-300">
            <span class="text-blue-400">AI</span>BS
        </a>
    </div>

    <!-- Desktop menu right
    <div class=" md:block">
        <div class="ml-4 flex items-center md:ml-6 space-x-2">
            <a href="{{ path('login') }}"
               class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:bg-slate-800/50 hover:scale-105"
               :class="{ 'text-white bg-slate-800/50': app.request.attributes.get('_route') == 'login' }">
                Login
            </a>
            <a href="{{ path('demo') }}"
               class="ml-2 px-4 py-2 border border-transparent rounded-md text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 transition-all duration-300 transform hover:scale-105">
                Request Demo
            </a>
        </div>
    </div>-->
</div>

    <!-- Mobile menu -->
    <div x-show="isOpen"
         x-transition.opacity
         x-cloak
         @click.outside="isOpen = false"
         class="fixed inset-0 bg-slate-900/95 backdrop-blur-md md:hidden">
        <div class="flex h-full flex-col items-center justify-center space-y-8">
            <a href="#" @click="isOpen = false" class="text-2xl text-white">Home</a>
            <a href="#" @click="isOpen = false" class="text-2xl text-white">About</a>
            <a href="#" @click="isOpen = false" class="text-2xl text-white">Services</a>
        </div>
    </div>

    <!-- Desktop menu
    <div class="hidden space-x-6 xxl:flex">
        <a href="#" class="text-gray-300 hover:text-white">Home</a>
        <a href="#" class="text-gray-300 hover:text-white">About</a>
        <a href="#" class="text-gray-300 hover:text-white">Services</a>
    </div>-->
</div>
