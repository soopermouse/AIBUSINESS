<header class="relative z-50">
    <nav class="bg-slate-900/80 backdrop-blur-md border-b border-slate-700/50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <!-- Mobile menu button -->
                <div class="flex items-center md:hidden">
                    <button @click="open = !open" type="button" class="inline-flex items-center justify-center p-2 rounded-md text-slate-400 hover:text-white hover:bg-slate-700 focus:outline-none">
                        <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>

                <!-- Desktop menu -->
                <div class="hidden md:block">
                    <div class="flex items-center space-x-4">
                        <a href="{{ path('home') }}" class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-300">Home</a>
                        <a href="{{ path('about') }}" class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-300">About</a>
                        <a href="{{ path('services') }}" class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-300">Services</a>
                        <a href="{{ path('contact') }}" class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-300">Contact</a>
                    </div>
                </div>

                <!-- Logo / Brand -->
                <div class="flex-shrink-0 flex items-center">
                    <a href="{{ path('home') }}" class="text-xl font-bold metallic-text">AI<span class="text-blue-400">BS</span></a>
                </div>

                <!-- Right side items -->
                <div class="hidden md:block">
                    <div class="ml-4 flex items-center md:ml-6">
                        <a href="{{ path('home') }}" class="text-slate-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-300">Login</a>
                        <a href="{{ path('home') }}" class="ml-4 px-4 py-2 border border-transparent rounded-md text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 transition-colors duration-300">Request Demo</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Mobile menu -->
        <div x-show="open" class="md:hidden" x-transition:enter="transition ease-out duration-100" x-transition:leave="transition ease-in duration-75">
            <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
                <a href="{{ path('home') }}" class="text-slate-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Home</a>
                <a href="{{ path('about') }}" class="text-slate-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">About</a>
                <a href="{{ path('services') }}" class="text-slate-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Services</a>
                <a href="{{ path('contact') }}" class="text-slate-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Contact</a>
                <div class="pt-4 border-t border-slate-700">
                    <a href="{{ path('home') }}" class="text-slate-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Login</a>
                    <a href="{{ path('home') }}" class="mt-2 block w-full px-5 py-3 text-center rounded-md bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors duration-300">Request Demo</a>
                </div>
            </div>
        </div>
    </nav>
</header>
